# 前端分支合并审查 · 2026-09-08

## 取舍

`main` (`5b5e00c`) 是早期骨架；`feat/moonpool-native-app` (`57f8241`) 是其连续开发后继，领先 14 个提交，main 没有独有提交。不存在两套并列的成熟前端需要二选一。保留功能分支最终状态作为新的 main 开发基线，不拆散互相依赖的设计组件、状态模型与界面。

| 内容 | 决定 | 原因 |
| --- | --- | --- |
| 原生月池、常曦素材、月相、统一组件 | 合并 | 已实现，提供一致的品牌与前端基础，但视觉仍需打磨 |
| 常驻导航、聊天输入、医生示例回复入口、确认回填 | 合并 | UI 流程实测通过；保留用户要求的常驻导航 |
| 本地健康记录、用药、计划、报告、预约意向 | 合并 | 本地流程与持久化有测试；不宣称已提交医疗机构 |
| HTTP / JWT / SSE / 文档 / 语音等适配器 | 保留代码，不盲目开启 | 有复用价值，但适配器存在不等于真实服务器端到端可用 |
| “完整在线医疗助手”“全部后端能力已打通”的表述 | 不采纳 | 当前聊天使用事件接口；扩展能力开关关闭；真实模型与真机网络未验收 |
| 本机未提交修改、`codex/local-ui-checkpoint-20260908` | 不混入 | 不属于这次远端分支基线，保留原样，避免覆盖或引入未审查修改 |

## 视觉审查

实看：首页、聊天、健康、服务截图，并导出完整 UI 流程附件。整体比骨架完整，设计系统、图片资产、输入区与导航值得保留，但还不是精修定稿。

| Before | After（后续设计建议，本次未重做） | What this changes |
| --- | --- | --- |
| 首页 `MoonPoolView` 高 292pt，加大标题，占首屏较多 | 后续试验 252pt，并比较大字模式；本次保留已通过流程的布局 | 给“今天”内容更多首屏空间 |
| 水面仍以椭圆基底和同心涟漪为主 | 后续增加局部接触波纹与轻倒影，保留 Reduce Motion 静态版本 | 减少托盘感，更接近水面 |
| 聊天空态三条建议之间及下方留白偏多 | 后续将独立建议行纵向间距统一为 16pt，再做键盘与大字截图验收 | 内容更紧凑，输入意图更清楚 |

## 验证边界

独立 worktree 构建 `57f8241`，未使用主工作区未提交修改。iPhone 17 Pro / iOS 26.5 Simulator，完整 `xcodebuild test`：59 passed、0 failed、0 skipped（55 单元，4 UI）。UI 包括核心导航/聊天、记录/服务、大字/横屏、医生回复/血压回填。

结果：`/private/tmp/changxi-merge-validation.xcresult`；截图：`/private/tmp/changxi-merge-screenshots`（本机临时产物，不随 Git 发布）。测试是离线/Mock，不构成真实 AI、线上医生、跨设备同步或医疗正确性的证明。

合并审查更正了后端分支说明和三处界面文案：玄同最新为 `master` (`9f6295b`)，不能继续宣称整个后端“没有认证/文档接口”。开关不变，等待独立集成验证。保留所有原分支和提交历史，不删除分支，不强制推送。
